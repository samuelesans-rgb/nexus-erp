{
  "name": "@nexus-erp/kitchen-connector-standalone",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Nexus Kitchen Connector runtime for Linux and Termux",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && tsc -p tsconfig.json && node scripts/finalize-build.mjs",
    "postinstall": "npm run build",
    "start": "node dist/kitchen-connector/fusion.js",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.19.24",
    "typescript": "^5.9.3"
  }
}
