import DocumentForm from "../document-form"; import { getDocumentOptions } from "@/lib/documents"; import { requireDocumentContext } from "@/lib/document-access";
export default async function NewDocumentPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { companyId, locationId } = await requireDocumentContext(true); const [options, query] = await Promise.all([getDocumentOptions(companyId, locationId), searchParams]); return <div className="space-y-5"><h1 className="text-3xl font-bold">Nuovo documento</h1>{query.error && <p className="rounded-lg bg-red-50 p-3 text-red-700">{query.error}</p>}<DocumentForm options={options}/></div>; }
