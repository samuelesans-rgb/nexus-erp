import DocumentForm from "../../document-form"; import { getDocument, getDocumentOptions } from "@/lib/documents"; import { requireDocumentContext } from "@/lib/document-access"; import { notFound, redirect } from "next/navigation";
export default async function EditDocumentPage({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ error?: string }> }) { const { companyId } = await requireDocumentContext(true); const [{ id }, query] = await Promise.all([params, searchParams]); const [document, options] = await Promise.all([getDocument(companyId, id), getDocumentOptions(companyId)]); if (!document) notFound(); if (document.status !== "DRAFT") redirect(`/documents/${id}`); return <div className="space-y-5"><h1 className="text-3xl font-bold">Modifica {document.documentNumber}</h1>{query.error && <p className="rounded-lg bg-red-50 p-3 text-red-700">{query.error}</p>}<DocumentForm options={options} document={document}/></div>; }
