import{requireRestaurantContext}from"@/lib/restaurant-access";import{MODULE_CODES}from"@/lib/module-catalog";import{getRestaurantOptions}from"@/lib/restaurant";import{createReservationAction}from"../../actions";
export default async function Page(){const{companyId,locationId}=await requireRestaurantContext(MODULE_CODES.RESTAURANT_RESERVATIONS,"operate");const o=await getRestaurantOptions(companyId,locationId);return <><h1 className="text-2xl font-bold">Nuova prenotazione</h1><form action={createReservationAction} className="grid gap-3 rounded border p-5 md:grid-cols-2"><select name="locationId">{o.locations.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select><select name="partnerId"><option value="">Ospite non censito</option>{o.partners.map(x=><option value={x.id} key={x.id}>{x.displayName||x.name}</option>)}</select><input name="guestName" placeholder="Nome ospite" required/><input name="phone" placeholder="Telefono"/><input name="email" type="email" placeholder="Email"/><input name="startTime" type="datetime-local" required/><input name="partySize" type="number" min="1" defaultValue="2"/><select name="source">{["PHONE","WALK_IN","WEBSITE","MANUAL","THIRD_PARTY"].map(x=><option key={x}>{x}</option>)}</select><select name="status"><option>PENDING</option><option>WAITLIST</option><option>CONFIRMED</option></select><select name="tableIds" multiple>{o.tables.map(x=><option value={x.id} key={x.id}>{x.name} ({x.seats})</option>)}</select><textarea name="notes" placeholder="Note"/><label><input name="adminOverride" type="checkbox"/> Override sovrapposizione (Admin)</label><button className="rounded bg-slate-900 p-2 text-white">Crea</button></form></>}
