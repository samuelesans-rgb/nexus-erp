import { randomUUID } from "node:crypto";import { requireRestaurantContext } from "@/lib/restaurant-access";import { MODULE_CODES } from "@/lib/module-catalog";import { getPrintQueue } from "@/lib/restaurant-kitchen";import { kitchenPrintAction } from "../../actions";
export default async function Page(){const c=await requireRestaurantContext(MODULE_CODES.RESTAURANT_KITCHEN,"kitchen");const jobs=await getPrintQueue(c.companyId,c.locationId);return <div className="space-y-4"><h1 className="text-2xl font-bold">Coda stampa cucina</h1><div className="grid gap-3">{jobs.map(j=><article className="rounded border p-4" key={j.id}><div className="flex flex-wrap justify-between gap-2"><b>{j.station.name} · {j.ticket.orderCode} · #{j.ticket.dispatchNumber}</b><span>{j.type} · {j.status} · tentativi {j.attempts}</span></div>{j.lastError&&<p className="text-red-700">{j.lastError}</p>}<div className="mt-2 flex gap-2">{j.status==="PENDING"&&<form action={kitchenPrintAction}><input type="hidden" name="jobId" value={j.id}/><button className="rounded border px-3 py-1" name="operation" value="process">Stampa</button></form>}{j.status==="FAILED"&&<form action={kitchenPrintAction}><input type="hidden" name="jobId" value={j.id}/><button className="rounded border px-3 py-1" name="operation" value="retry">Retry</button></form>}{j.status==="PRINTED"&&<form action={kitchenPrintAction}><input type="hidden" name="ticketId" value={j.ticketId}/><input type="hidden" name="idempotencyKey" value={randomUUID()}/><button className="rounded border px-3 py-1" name="operation" value="reprint">Ristampa</button></form>}</div></article>)}</div></div>}
