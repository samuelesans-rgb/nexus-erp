import {requireRestaurant}from"@/lib/restaurant-access";import{getRestaurantOptions}from"@/lib/restaurant";import{saveTableAction}from"../actions";
export default async function Page(){const{companyId}=await requireRestaurant("manage");const o=await getRestaurantOptions(companyId);return <><h1 className="text-2xl font-bold">Tavoli</h1><form action={saveTableAction} className="grid gap-3 rounded border p-4 md:grid-cols-4"><select name="locationId">{o.locations.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select><select name="areaId">{o.areas.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select><input name="code" placeholder="Codice" required/><input name="name" placeholder="Nome" required/><input name="seats" type="number" min="1" defaultValue="2"/><select name="status">{["AVAILABLE","RESERVED","OCCUPIED","DIRTY","OUT_OF_SERVICE"].map(x=><option key={x}>{x}</option>)}</select><button className="rounded bg-slate-900 p-2 text-white">Salva tavolo</button></form>{o.tables.map(x=><p className="border-b p-3" key={x.id}>{x.code} · {x.name} · {x.seats} posti · {x.status}</p>)}</>}
