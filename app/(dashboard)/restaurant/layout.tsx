import Link from "next/link";import { requireRestaurant } from "@/lib/restaurant-access";import { RESTAURANT_ROUTES } from "@/lib/restaurant-routing";
export default async function Layout({children}:{children:React.ReactNode}){await requireRestaurant();return <div className="space-y-6"><nav className="flex flex-wrap gap-2 border-b pb-3 text-sm">{Object.entries(RESTAURANT_ROUTES).map(([label,href])=><Link className="rounded px-3 py-2 hover:bg-slate-100" href={href} key={href}>{label}</Link>)}</nav>{children}</div>}
