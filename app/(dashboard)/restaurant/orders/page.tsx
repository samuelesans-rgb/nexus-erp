import Link from"next/link";import{requireRestaurantContext}from"@/lib/restaurant-access";import{MODULE_CODES}from"@/lib/module-catalog";import{getOrders}from"@/lib/restaurant-orders";
export default async function Page(){const{companyId,locationId}=await requireRestaurantContext(MODULE_CODES.RESTAURANT_FLOOR);const rows=await getOrders(companyId,locationId);return <><div className="flex justify-between"><h1 className="text-2xl font-bold">Comande</h1><Link className="rounded bg-slate-900 p-2 text-white" href="/restaurant/orders/new">Nuova</Link></div>{rows.map(x=><Link className="block border-b p-3" href={`/restaurant/orders/${x.id}`} key={x.id}>{x.code} · {x.table?.name||x.serviceType} · {x.status} · {x._count.lines} righe · {x.paymentStatus}</Link>)}</>}
