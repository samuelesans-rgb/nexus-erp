import{requireRestaurantContext}from"@/lib/restaurant-access";import{MODULE_CODES}from"@/lib/module-catalog";import{getRestaurantOptions}from"@/lib/restaurant";import{openOrderAction}from"../../actions";
export default async function Page(){const{companyId,locationId}=await requireRestaurantContext(MODULE_CODES.RESTAURANT_FLOOR,"operate");const o=await getRestaurantOptions(companyId,locationId);return <><h1 className="text-2xl font-bold">Nuova comanda</h1><form action={openOrderAction} className="grid gap-3 rounded border p-5 md:grid-cols-2"><select name="locationId">{o.locations.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select><select name="tableId"><option value="">Nessun tavolo</option>{o.tables.filter(x=>x.status==="AVAILABLE").map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select><select name="partnerId"><option value="">Cliente da assegnare</option>{o.partners.map(x=><option value={x.id} key={x.id}>{x.displayName||x.name}</option>)}</select><input name="guestCount" type="number" min="1" defaultValue="2"/><select name="serviceType">{["DINE_IN","TAKEAWAY","DELIVERY","EVENT"].map(x=><option key={x}>{x}</option>)}</select><textarea name="notes"/><button className="rounded bg-slate-900 p-2 text-white">Apri comanda</button></form></>}
