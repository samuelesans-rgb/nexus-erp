import{requireRestaurantContext}from"@/lib/restaurant-access";import{MODULE_CODES}from"@/lib/module-catalog";import{getRestaurantOptions}from"@/lib/restaurant";import{createMenuAction}from"../../actions";
export default async function Page(){const{companyId}=await requireRestaurantContext(MODULE_CODES.RESTAURANT_MENU,"manage");const o=await getRestaurantOptions(companyId);return <><h1 className="text-2xl font-bold">Nuovo menu</h1><form action={createMenuAction} className="grid gap-3 rounded border p-5"><select name="locationId"><option value="">Tutte le sedi</option>{o.locations.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select><input name="code" placeholder="Codice" required/><input name="name" placeholder="Nome" required/><textarea name="description"/><button className="rounded bg-slate-900 p-2 text-white">Crea</button></form></>}
