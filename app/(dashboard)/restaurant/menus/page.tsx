import Link from"next/link";import{requireRestaurantContext}from"@/lib/restaurant-access";import{MODULE_CODES}from"@/lib/module-catalog";import{prisma}from"@/lib/prisma";
export default async function Page(){const{companyId}=await requireRestaurantContext(MODULE_CODES.RESTAURANT_MENU);const rows=await prisma.restaurantMenu.findMany({where:{companyId,deletedAt:null},include:{_count:{select:{sections:true}}}});return <><div className="flex justify-between"><h1 className="text-2xl font-bold">Menu</h1><Link className="rounded bg-slate-900 p-2 text-white" href="/restaurant/menus/new">Nuovo</Link></div>{rows.map(x=><Link className="block border-b p-3" href={`/restaurant/menus/${x.id}`} key={x.id}>{x.code} · {x.name} · {x._count.sections} sezioni</Link>)}</>}
