import { createAccountAction } from "../../actions"; import { Header, inputClass } from "../../treasury-ui"; import { requireTreasuryContext } from "@/lib/treasury-access";
export default async function Page() { await requireTreasuryContext("manage"); return <><Header title="Nuovo conto" subtitle="Configura un conto finanziario tenant-scoped." /><form action={createAccountAction} className="grid gap-4 rounded border bg-white p-6 md:grid-cols-2"><label>Codice<input className={inputClass} name="code" required /></label><label>Nome<input className={inputClass} name="name" required /></label><label>Tipo<select className={inputClass} name="type">{["BANK","CASH","CARD","PAYPAL","OTHER"].map(v => <option key={v}>{v}</option>)}</select></label><label>Valuta<input className={inputClass} defaultValue="EUR" maxLength={3} name="currency" /></label><label>Saldo iniziale<input className={inputClass} defaultValue="0" name="openingBalance" type="number" step="0.01" /></label><label>IBAN<input className={inputClass} name="iban" /></label><label>BIC<input className={inputClass} name="bic" /></label><label>Banca<input className={inputClass} name="bankName" /></label><label className="flex gap-2"><input name="allowOverdraft" type="checkbox" />Consenti scoperto</label><button className="rounded bg-slate-900 px-4 py-2 text-white md:col-span-2">Salva</button></form></>; }
