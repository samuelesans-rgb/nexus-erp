import{getDocumentOptions}from"@/lib/documents";import{requirePurchasingContext}from"@/lib/purchasing-access";import{notFound}from"next/navigation";import Form from"../purchase-form";export default async function Page({params,searchParams}:{params:Promise<{kind:string}>;searchParams:Promise<{error?:string}>}){const[{kind},q]=await Promise.all([params,searchParams]);if(kind!=="orders")notFound();const{companyId}=await requirePurchasingContext("order");const options=await getDocumentOptions(companyId);return <div className="space-y-5"><h1 className="text-3xl font-bold">Nuovo ordine fornitore</h1>{q.error&&<p className="bg-red-50 p-3 text-red-700">{q.error}</p>}<Form options={options}/></div>;}
