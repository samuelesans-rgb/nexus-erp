import{getDocument,getDocumentOptions}from"@/lib/documents";import{requirePurchasingContext}from"@/lib/purchasing-access";import{notFound}from"next/navigation";import Form from"../../purchase-form";export default async function Page({params,searchParams}:{params:Promise<{kind:string;id:string}>;searchParams:Promise<{error?:string}>}){const[{kind,id},q]=await Promise.all([params,searchParams]);if(kind!=="orders")notFound();const{companyId,locationId}=await requirePurchasingContext("order");const[document,options]=await Promise.all([getDocument(companyId,locationId,id),getDocumentOptions(companyId,locationId)]);if(!document||document.documentType!=="PURCHASE_ORDER"||document.status!=="DRAFT")notFound();return <div className="space-y-5"><h1 className="text-3xl font-bold">Modifica ordine</h1>{q.error&&<p className="bg-red-50 p-3">{q.error}</p>}<Form options={options} document={document}/></div>;}
