import type {DocumentType} from "@/generated/prisma/client";
export const PURCHASE_ROUTES={orders:{type:"PURCHASE_ORDER",label:"Ordini fornitori",singular:"Ordine fornitore"},receipts:{type:"GOODS_RECEIPT",label:"Ricevimenti",singular:"Ricevimento"},invoices:{type:"PURCHASE_INVOICE",label:"Fatture passive",singular:"Fattura passiva"},returns:{type:"RETURN",label:"Resi fornitori",singular:"Reso fornitore"}} as const satisfies Record<string,{type:DocumentType;label:string;singular:string}>;
export type PurchaseKind=keyof typeof PURCHASE_ROUTES;export function purchaseRoute(kind:string){return PURCHASE_ROUTES[kind as PurchaseKind];}export function kindForPurchaseType(type:DocumentType){return(Object.entries(PURCHASE_ROUTES).find(([,value])=>value.type===type)?.[0]??"orders") as PurchaseKind;}
