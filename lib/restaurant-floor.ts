import "server-only";
import { prisma } from "@/lib/prisma";
export class RestaurantFloorError extends Error{constructor(message:string){super(message);this.name="RestaurantFloorError"}}
export async function saveTableCombination(companyId:string,locationId:string,input:{id?:string;name:string;tableIds:string[];active:boolean}){const ids=[...new Set(input.tableIds)];if(ids.length<2)throw new RestaurantFloorError("Una combinazione richiede almeno due tavoli.");return prisma.$transaction(async tx=>{const tables=await tx.restaurantTable.findMany({where:{companyId,locationId,id:{in:ids},active:true,deletedAt:null,combinable:true},select:{id:true,areaId:true}});if(tables.length!==ids.length||new Set(tables.map(t=>t.areaId)).size!==1)throw new RestaurantFloorError("I tavoli devono essere combinabili e appartenere alla stessa area e sede.");const existing=await tx.restaurantTableCombination.findFirst({where:{companyId,locationId,name:input.name.trim(),id:input.id?{not:input.id}:undefined}});if(existing)throw new RestaurantFloorError("Nome combinazione già utilizzato.");if(input.id){const updated=await tx.restaurantTableCombination.updateMany({where:{id:input.id,companyId,locationId},data:{name:input.name.trim(),active:input.active}});if(!updated.count)throw new RestaurantFloorError("Combinazione non trovata.");await tx.restaurantTableCombinationTable.deleteMany({where:{companyId,combinationId:input.id}});await tx.restaurantTableCombinationTable.createMany({data:ids.map(tableId=>({companyId,combinationId:input.id!,tableId}))});return{id:input.id}}const row=await tx.restaurantTableCombination.create({data:{companyId,locationId,areaId:tables[0].areaId,name:input.name.trim(),active:input.active},select:{id:true}});await tx.restaurantTableCombinationTable.createMany({data:ids.map(tableId=>({companyId,combinationId:row.id,tableId}))});return row})}
export async function dissolveTableCombination(companyId:string,locationId:string,id:string){return prisma.$transaction(async tx=>{const combination=await tx.restaurantTableCombination.findFirst({where:{id,companyId,locationId,active:true},include:{tables:true}});if(!combination)throw new RestaurantFloorError("Combinazione non trovata.");const tableIds=combination.tables.map(row=>row.tableId);const [orderConflict,reservationConflict]=await Promise.all([tx.restaurantOrderTable.findFirst({where:{companyId,locationId,tableId:{in:tableIds},order:{status:{notIn:["CLOSED","CANCELLED"]}}}}),tx.restaurantReservationTable.findFirst({where:{companyId,tableId:{in:tableIds},reservation:{locationId,deletedAt:null,status:{in:["PENDING","CONFIRMED","SEATED"]},endTime:{gt:new Date()}}}})]);if(orderConflict||reservationConflict)throw new RestaurantFloorError("La combinazione è utilizzata da una comanda o prenotazione attiva.");await tx.restaurantTableCombination.update({where:{id},data:{active:false}});return{id};});}
